using System.Collections;
using System.Collections.Generic;
using UnityEngine;



public partial class Train : MonoBehaviour {
   /* System.Random rand = new System.Random();
    public bool Awaria = false;
    public bool Choroba = false;
    public bool EventRand = false;
    

    public void Event()
    {
        int los = rand.Next(0, 10000);
        if (los <= 50)
        {
            Awaria = true;
            gen.Emergency = true;
            
    .Log("Awaria");
        }
        else if (los >= 9950)
        {
            Choroba = true;
            gen.Emergency = true;
            Debug.Log("Paszer");
        }
        else Debug.Log("Nic sie nie stalo");
        EventRand = true;
        
    }
	*/
}
